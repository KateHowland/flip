<flipui:ConditionalControl x:Class="Sussex.Flip.UI.IfControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:flipui="clr-namespace:Sussex.Flip.UI"
	Background="Purple">
	
	<flipui:ConditionalControl.Resources>
	
		<Style TargetType="TextBlock">
			<Setter Property="TextBlock.FontSize" Value="14"/>
			<Setter Property="TextBlock.Foreground" Value="Green"/>
			<Setter Property="TextBlock.Background" Value="Black"/>
			<Setter Property="TextBlock.Height" Value="30"/>
			<Setter Property="TextBlock.Width" Value="100"/>
		</Style>
	
	</flipui:ConditionalControl.Resources>

	<Grid x:Name="grid">
		
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
	
		<TextBlock
		Grid.Row="0"
		Grid.Column="0"
		Text="If"/>
	
		<TextBlock
		Grid.Row="2"
		Grid.Column="0"
		Text="Then"/>
	
	</Grid>

</flipui:ConditionalControl>