<flipui:Moveable x:Class="Sussex.Flip.UI.EventBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:flipui="clr-namespace:Sussex.Flip.UI"
	Height="30"
	Width="140"
	DataContext="{Binding RelativeSource={RelativeSource Self}}">
	
	<UserControl.Resources>
		<LinearGradientBrush 
		x:Key="redGradient"
		StartPoint="0,0"
		EndPoint="1,1">
			<LinearGradientBrush.GradientStops>
				<GradientStop Offset="0" Color="Maroon"/>
				<GradientStop Offset="0.5" Color="Firebrick"/>
				<GradientStop Offset="1" Color="Maroon"/>
			</LinearGradientBrush.GradientStops>
		</LinearGradientBrush>
	</UserControl.Resources>
	
    <Border
	VerticalAlignment="Stretch"
	HorizontalAlignment="Stretch"
	BorderThickness="1"
	BorderBrush="Black"
	CornerRadius="2,20,2,20"
	Background="{StaticResource redGradient}"
	ToolTip="{Binding Path=EventName}">
	
		<TextBlock
    	Text="{Binding Path=DisplayName}"
		Foreground="Ivory"
    	FontSize="14"
		FontWeight="Bold"
    	TextAlignment="Center"
    	VerticalAlignment="Center"
    	HorizontalAlignment="Center"
		/>
	
	</Border>

</flipui:Moveable>